# ===========================================
# Secure Task Management System - Environment
# ===========================================
# Copy to .env and adjust. Never commit .env.

# ---------- API (NestJS) ----------
PORT=3333
NODE_ENV=development

# ---------- JWT ----------
JWT_SECRET=your-super-secret-key-min-32-chars
JWT_EXPIRES=7d

# ---------- Database ----------
# Use SQLite (default) or PostgreSQL
DB_TYPE=sqlite
DB_DATABASE=data/myapp.sqlite

# PostgreSQL (when DB_TYPE=postgres)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=yourpassword
# DB_DATABASE=taskmgmt

# Optional
# DB_LOGGING=false

# ---------- Seed (first-run default user) ----------
SEED_USER_EMAIL=admin@example.com
SEED_USER_PASSWORD=Admin123!
